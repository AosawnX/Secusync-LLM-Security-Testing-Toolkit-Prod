fastapi==0.115.0
uvicorn==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
openai
anthropic
pytest
pytest-asyncio
# Sprint 2 — Mutation Engine
sentence-transformers==3.0.1  # embeddings for FAISS dedup
faiss-cpu==1.8.0.post1        # vector similarity dedup (architecture.md §3.2)
# NOTE: if faiss/sentence-transformers fail to install, engine falls back
# to difflib string-ratio dedup (logged at runtime). See History.md.
